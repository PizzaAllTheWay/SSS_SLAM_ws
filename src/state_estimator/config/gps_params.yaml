/**:
  ros__parameters:
    gps:
      # This is more for converting form GPS frame to local frame
      frame_orientation: [0.0, 0.0, 0.0]
      

      # Lever arm position of GPS in body frame [m]
      position: [-0.283, 0.000, -0.287]
